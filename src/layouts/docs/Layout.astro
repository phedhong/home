---

import PlaceHolder from "./PlaceHolder.astro";

const itemList = new Array(8).fill(undefined);
---

<html lang="en">
  
  <head></head>
  <body>

    <div class="min-h-[5vh]"><slot name="header" /></div>

    <div class="flex min-h-[80vh] flex-col md:flex-row">
      
      <div class="relative flex min-h-[80vh] min-w-[80vw] shrink-0 bg-gray-200 grow-2 items-center justify-center p-12">
        <p class="absolute p-2 text-5xl text-white/10">BackGround</p>
        
        <div
          role="status"
          class="w-full space-y-4 divide-y divide-gray-700 rounded border border-gray-700 p-4 shadow md:p-6"
        >
          Card
          {itemList.map(() => <PlaceHolder />)}
        </div>
      </div>

      <div class="relative min-h-[80vh] shrink-0 grow basis-10 p-12" >
        <div class="md:h-[55vh]">Aside Top</div>

        <aside class="rounded-md border border-slate-500 p-4">
          <slot>
            <h1 class="text-white/20">
              <b>Slot: {" "}</b>your island goes here
            </h1>
          </slot>
        </aside>
        <p class="absolute p-2 text-xl text-white/30">Interactive content</p>
      </div>
    
    </div>

    <div class="min-h-[5vh]"><slot name="footer" /></div>
  </body>
</html>

