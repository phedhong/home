---

export const prerender = true;
import Layout from "@layouts/default/Layout.astro";
import { categories } from "@scripts/config";
import { GET, ALL } from '../api/todos.ts';
import { getDataEntryById, getEntries, getEntryBySlug,getEntry } from 'astro:content';



// const ShoppingCart = await getDataEntryById('example', 'cart');


  export async function getStaticPaths()  {
    
    return categories.flatMap((category) => {


      return {
        params: {category}      }
    });
    
  }

  // const response = await GET(Astro)
  // const data = await response.json();

  const  { category } = Astro.params
  // const {filteredCategories} = Astro.props


---

<Layout>


  { category }


</Layout>